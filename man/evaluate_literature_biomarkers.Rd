% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_literature_biomarkers.R
\name{evaluate_literature_biomarkers}
\alias{evaluate_literature_biomarkers}
\title{Evaluate Predefined Literature Biomarkers}
\usage{
evaluate_literature_biomarkers(
  lit_biomarker_file_path = system.file("files", "literature_biomarkers.yaml", package =
    "biodiscvr"),
  prepared_data_list,
  config,
  datasets_to_evaluate = names(prepared_data_list),
  groups_to_evaluate = c("CU", "CI"),
  calculate_ci = FALSE,
  nsim = 1000,
  output_csv_path = NULL,
  id_col = NULL,
  verbose = TRUE
)
}
\arguments{
\item{lit_biomarker_file_path}{Path to literature biomarkers file. A named list where each element defines a
literature biomarker. Each element must be a list containing at least:
\verb{$name} (character), \verb{$numerator_regions} (character vector),
\verb{$denominator_regions} (character vector), and \verb{$var_composition} (numeric, 0-3, or -1 if calculation is not traditional SUVR).
Optionally include \verb{$description} and \verb{$source_citation}.}

\item{prepared_data_list}{List. The output from \code{preprocess_datasets()},
containing filtered data for multiple datasets.}

\item{config}{List. The loaded main configuration object.}

\item{datasets_to_evaluate}{Character vector. Names of the datasets within
\code{prepared_data_list} to evaluate the biomarkers on. Defaults to using all.}

\item{groups_to_evaluate}{Character vector. Groups ("CU", "CI") to evaluate.
Defaults to \code{c("CU", "CI")}.}

\item{calculate_ci}{Logical. Calculate 95\% CIs? Defaults to \code{FALSE}.}

\item{nsim}{Integer. Number of bootstrap simulations if \code{calculate_ci} is \code{TRUE}.
Defaults to 100.}

\item{output_csv_path}{Character string or NULL. Path to save the evaluation
results as a \emph{new} CSV file (overwrites if exists).}

\item{id_col}{Character string. Name of the unique ID column. Defaults to "RID".}

\item{verbose}{Logical. Print progress messages? Defaults to TRUE.}
}
\value{
A data frame containing the evaluation results. Each row corresponds to
one literature biomarker evaluated on one specific dataset and group.
Returns \code{NULL} if essential data is missing or no biomarkers can be evaluated.
}
\description{
Calculates and evaluates the performance (Rep, SepAB, SSE) of a predefined
set of literature-based biomarkers across specified datasets and groups.
}
\details{
Iterates through each literature biomarker definition and each specified
dataset/group. For each combination, it checks if the required regions exist
in the dataset, calculates the composite biomarker value using a simplified
internal helper (\code{.calculate_value_from_regions}), prepares the data, checks
for sufficient samples, and calls the appropriate evaluation function
(\code{.feval_group} or \code{.feval_group_95CI}). Results are aggregated and optionally saved.
}
